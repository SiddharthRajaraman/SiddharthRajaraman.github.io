function smoothScrollProjects() {
  var element = document.querySelector(".cardView");

  // scroll to element
  element.scrollIntoView();
}
function smoothScrollAboutMe() {
  var element = document.querySelector(".aboutMe");

  // scroll to element
  element.scrollIntoView();
}
function smoothScrollGoUp() {
  var element = document.querySelector(".goUp");

  // scroll to element
  element.scrollIntoView();
}
function smoothScrollContact() {
  var element = document.querySelector(".contactMe");

  // scroll to element
  element.scrollIntoView();
}
